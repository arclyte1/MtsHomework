package org.example;

import java.math.BigDecimal;

public class Cat extends Pet {

    Cat(String breed, String name, BigDecimal cost, String character) {
        super(breed, name, cost, character);
    }
}
