package org.example;

import java.math.BigDecimal;

public class Shark extends Predator {

    Shark(String breed, String name, BigDecimal cost, String character) {
        super(breed, name, cost, character);
    }
}
